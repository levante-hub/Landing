import type {
  MCPConfiguration,
  MCPConfigurationField,
  MCPConfigurationTemplate,
  MCPInputField,
  MCPCategory,
  MCPServer,
  MCPTransport,
} from '@/types/mcp';

// Alias usado en componentes nuevos; coincide con MCPServer existente
export type MCPServerExtended = MCPServer;

export type {
  MCPConfiguration,
  MCPConfigurationField,
  MCPConfigurationTemplate,
  MCPInputField,
  MCPCategory,
  MCPServer,
  MCPTransport,
};
